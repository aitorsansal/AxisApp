<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
	<!-- allowBackup off (SECURITY_AUDIT.md #10): SecureStorage's preferences file holds the Supabase
	     session and would be copied into cloud backups, then be undecryptable on the restored
	     device (its AES key lives in the Keystore and never leaves). The only other local state is
	     per-device display preferences that are deliberately not synced anyway. -->
	<application android:allowBackup="false" android:icon="@mipmap/appicon" android:roundIcon="@mipmap/appicon_round" android:supportsRtl="true" android:enableOnBackInvokedCallback="false"></application>
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.CAMERA" />
	<uses-feature android:name="android.hardware.camera" android:required="false" />
	<!-- Android 13+ (API 33) requires this as a runtime-requested permission, not just this
	     manifest entry, before a notification can show at all — see PushRegistrationService. -->
	<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
</manifest>
