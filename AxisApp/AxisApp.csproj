<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<TargetFrameworks>net10.0-android</TargetFrameworks>
		<TargetFrameworks Condition="$([MSBuild]::IsOSPlatform('windows'))">$(TargetFrameworks);net10.0-windows10.0.19041.0</TargetFrameworks>

		<!-- Note for MacCatalyst:
		The default runtime is maccatalyst-x64, except in Release config, in which case the default is maccatalyst-x64;maccatalyst-arm64.
		When specifying both architectures, use the plural <RuntimeIdentifiers> instead of the singular <RuntimeIdentifier>.
		The Mac App Store will NOT accept apps with ONLY maccatalyst-arm64 indicated;
		either BOTH runtimes must be indicated or ONLY macatalyst-x64. -->
		<!-- For example: <RuntimeIdentifiers>maccatalyst-x64;maccatalyst-arm64</RuntimeIdentifiers> -->

		<OutputType>Exe</OutputType>
		<RootNamespace>AxisApp</RootNamespace>
		<UseMaui>true</UseMaui>
		<SingleProject>true</SingleProject>
		<ImplicitUsings>enable</ImplicitUsings>
		<Nullable>enable</Nullable>

		<!-- Display name -->
		<ApplicationTitle>Axis</ApplicationTitle>

		<!-- App Identifier -->
		<ApplicationId>com.aitorsansal.axisapp</ApplicationId>

		<!-- Versions -->
		<ApplicationDisplayVersion>1.0.1</ApplicationDisplayVersion>
		<ApplicationVersion>8</ApplicationVersion>

		<!-- To develop, package, and publish an app to the Microsoft Store, see: https://aka.ms/MauiTemplateUnpackaged -->
		<WindowsPackageType>None</WindowsPackageType>

		<SupportedOSPlatformVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'android'">23.0</SupportedOSPlatformVersion>
		<SupportedOSPlatformVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'windows'">10.0.17763.0</SupportedOSPlatformVersion>
		<TargetPlatformMinVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'windows'">10.0.17763.0</TargetPlatformMinVersion>
	</PropertyGroup>

	<!--Keystore for always building with the same SHA1 — applied to Debug too (not just
	Release), so a Debug deploy signs with the same cert Google's OAuth client has registered.
	Without this, alternating Debug/Release deploys makes Android treat each as a different app
	(different signing cert) and demand an uninstall-confirm on every switch, and Google Sign-In
	fails outright on a Debug build since its signature was never registered anywhere. -->
	<Import Project="keystore.local.props" Condition="Exists('keystore.local.props')" />
	<PropertyGroup Condition="'$(TargetFramework)|$(Platform)'=='net10.0-android|AnyCPU'">
		<AndroidPackageFormat>apk</AndroidPackageFormat>
		<AndroidKeyStore>True</AndroidKeyStore>
	</PropertyGroup>

	<ItemGroup>
		<!-- App Icon -->
		<MauiIcon Include="Resources\AppIcon\appicon.svg" ForegroundFile="Resources\AppIcon\appiconfg.svg" Color="#0B1220" />

		<!-- Splash Screen -->
		<MauiSplashScreen Include="Resources\Splash\splash.svg" Color="#0B1220" BaseSize="128,128" />

		<!-- Images -->
		<MauiImage Include="Resources\Images\*" />

		<!-- Custom Fonts -->
		<MauiFont Include="Resources\Fonts\*" />

		<!-- Raw Assets (also remove the "Resources\Raw" prefix) -->
		<MauiAsset Include="Resources\Raw\**" LogicalName="%(RecursiveDir)%(Filename)%(Extension)" />
	</ItemGroup>

	<ItemGroup>
		<PackageReference Include="CommunityToolkit.Maui" Version="13.0.0" />
		<PackageReference Include="CommunityToolkit.Mvvm" Version="8.4.0" />
		<PackageReference Include="LiveChartsCore.SkiaSharpView.Maui" Version="2.0.5" />
		<PackageReference Include="Microsoft.Maui.Controls" Version="10.0.10" />
		<PackageReference Include="Microsoft.Extensions.Logging.Debug" Version="10.0.0" />
		<PackageReference Include="QRCoder" Version="1.8.0" />
		<!-- Downgraded from 4.151.1 to 3.119.0 (2026-09-15) to match the version
		     LiveChartsCore.SkiaSharpView.Maui's whole SkiaSharp.Views/.HarfBuzz/.Views.Maui.Controls
		     dependency chain resolves to — a 4.151.1 core against those still-3.119.0 companion
		     assemblies compiled fine (both sides expose stable public API) but broke at runtime:
		     confirmed live via logcat as a MethodAccessException in SkiaSharp.HarfBuzz.SKShaper.Shape
		     calling SKPaint.GetFont(), silently swallowed by LiveCharts' own internal render-error
		     handling ("[LiveCharts] chart update failed", never thrown) — every chart rendered as a
		     blank canvas with no error surfaced anywhere in the app itself. Explicitly pinning every
		     companion package up to 4.151.1 instead was rejected: SkiaSharp.Views.Maui.Controls
		     4.151.1 requires Microsoft.Maui.Controls >= 10.0.20 (this project pins 10.0.10), and
		     SkiaSharp.Views.WinUI/NativeAssets.WinUI don't ship an Android-compatible asset at all —
		     bumping the whole MAUI Controls version is a separate, bigger decision than this bug fix.
		     ImageResizer.cs (avatar/receipt resize) only uses long-stable core APIs (SKBitmap.Decode,
		     Resize, SKImage.FromBitmap, Encode) with nothing 4.x-specific — confirmed by a real build
		     + the same avatar-upload code path, not assumed. -->
		<PackageReference Include="SkiaSharp" Version="3.119.0" />
		<PackageReference Include="Supabase" Version="1.6.0" />
	</ItemGroup>

	<!-- Android Credential Manager + Google Identity, for native Google sign-in
	     (Services/Platforms/Android/GoogleAuthService.cs) — same packages/versions PokeCards
	     already uses for the identical flow. -->
	<ItemGroup Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'android'">
		<PackageReference Include="Xamarin.AndroidX.Credentials" Version="1.6.0.1" />
		<PackageReference Include="Xamarin.AndroidX.Credentials.PlayServicesAuth" Version="1.6.0.1" />
		<PackageReference Include="Xamarin.Google.Android.Libraries.Identity.GoogleId" Version="1.1.0.15" />
		<PackageReference Include="Xamarin.Firebase.Messaging" Version="125.1.1.1" />

		<!-- Pin: Xamarin.Firebase.Messaging pulls in Xamarin.AndroidX.Fragment 1.9.0
		     transitively, but Credential Manager's own Xamarin.AndroidX.Fragment.Ktx
		     dependency graph asks for a matching 1.8.8.1-series Fragment — the mismatch
		     merges two different-generation copies of the same AAR and duplicates
		     FragmentKt.class at the R8/dex step. Pinning Fragment.Ktx up to the 1.9.0
		     series it actually needs to pair with resolves it. -->
		<PackageReference Include="Xamarin.AndroidX.Fragment.Ktx" Version="1.9.0" />

		<!-- Firebase client config (project id/app id/API key — not a secret) — tells the
		     Firebase Android SDK which project to register push tokens against. The server-side
		     credential (a service account key, used to *send* pushes) is a different file and
		     never goes in this repo — see CLAUDE.md's push notifications section. -->
		<GoogleServicesJson Include="Platforms\Android\google-services.json" />
	</ItemGroup>
	
	
	<ItemGroup>
	  <Folder Include="keystore\" />
	</ItemGroup>

</Project>
